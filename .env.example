# 复制为 .env，向主管索取 DB_PASSWORD 后填写
DB_HOST=10.100.0.176
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=向主管索取
DB_NAME=cursor_team_reports

# 与 config/user.json 的 username 保持一致（可选，优先读 user.json）
CURSOR_REPORT_USER=你的名字
